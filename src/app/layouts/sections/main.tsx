import { Counter } from "../../../features/counter/counter"
import { PostsList } from "../../../features/posts/PostsList"

export const Main = () => {
    return (
        <main>
            main
            <Counter />
            <PostsList />
        </main>
    )
}
